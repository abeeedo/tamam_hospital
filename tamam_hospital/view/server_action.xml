<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>

        <record id="hospital_patients_action" model="ir.actions.server">
            <field name="name">Patients Server Actions</field>
            <field name="model_id" ref="tamam_hospital.model_hospital_patients"/>
            <field name="state">code</field>
            <field name="code">action = model.action_patients()</field>
        </record>


        <menuitem id="hospital_patients_server_action_menu"
                  name="Server Action"
                  parent="tamam_hospital"
                  action="hospital_patients_action"/>

    </data>
</odoo>